name: Create GitHub Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to publish'
        type: string
        required: true
      draft:
        description: 'Draft'
        type: boolean
        default: true
        required: true
  # Called by npm-publish.yml once it has pushed the tag. Reusable workflows
  # can't share an input block between triggers, so these mirror the ones above.
  workflow_call:
    inputs:
      tag:
        type: string
        required: true
      draft:
        type: boolean
        required: true

# `gh release create` writes to the repository. Naming it sets every other
# scope to none, whatever the repo-wide default happens to be.
permissions:
  contents: write

jobs:
  github-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.tag }}
          fetch-depth: 0
          fetch-tags: true
      - name: Create release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ inputs.tag }}
          DRAFT: ${{ inputs.draft }}
        run: |
          FLAGS=""
          if [ "$DRAFT" = 'true' ]; then FLAGS="$FLAGS --draft"; fi
          # A tag carrying a prerelease suffix (v4.0.0-rc.1) is marked as one,
          # which also keeps GitHub from labelling it the latest release.
          case "$RELEASE_TAG" in
            *-*) FLAGS="$FLAGS --prerelease" ;;
          esac
          git show -s --format=%b | \
          gh release create "$RELEASE_TAG" \
              --repo="$GITHUB_REPOSITORY" \
              --title="Version ${RELEASE_TAG#v}" \
              --verify-tag \
              $FLAGS \
              -F -
